# Project Instructions

## Git

- Before committing, verify the git user is `adityaviki` and email is `adityaviki01@gmail.com`:
  ```sh
  git config user.name   # should be adityaviki
  git config user.email  # should be adityaviki01@gmail.com
  ```
- Do not add a Co-Authored-By line to commits.
- Before pushing, ensure the active GitHub CLI account is `adityaviki`:
  ```sh
  gh auth switch --user adityaviki
  ```
